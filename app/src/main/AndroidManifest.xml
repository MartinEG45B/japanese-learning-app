<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.japanese_learning_app">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".News.MainActivity"
            android:label="Learn Japanese!"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                    <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VocabularyList.VocabularyListActivity"
            android:label="単語帳 / Vocabulary List"
            android:launchMode="singleTop">

        </activity>
        <activity android:name=".Dictionary.DictionaryActivity"
            android:label="辞書 / Dictionary"
            android:launchMode="singleTop">

        </activity>
        <activity android:name=".Camera.CameraActivity"
            android:label="カメラ / Camera"
            android:launchMode="singleTop">
        </activity>
    </application>

</manifest>